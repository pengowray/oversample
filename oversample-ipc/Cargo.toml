[package]
name = "oversample-ipc"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Shared IPC wire types (DTOs, enums, errors) for the Oversample frontend <-> Tauri backend boundary"

[dependencies]
serde = { version = "1", features = ["derive"] }
